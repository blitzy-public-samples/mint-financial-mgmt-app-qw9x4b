// This file is used to set up the Jest testing environment for the React frontend application.

// Import Jest DOM extensions to provide custom Jest matchers for asserting on DOM nodes
import '@testing-library/jest-dom';

// Add any custom global test setup here if needed

// Example of a custom global setup (commented out):
// beforeAll(() => {
//   // Set up global test environment
// });

// Example of a custom global teardown (commented out):
// afterAll(() => {
//   // Clean up global test environment
// });

// Human task (Optional):
// TODO: Verify if any custom global test setup is required for the project