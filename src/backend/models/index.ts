// Import models
import { UserModel } from './user.model';
import { AccountModel } from './account.model';
import { TransactionModel } from './transaction.model';
import { BudgetModel } from './budget.model';
import { GoalModel } from './goal.model';

// Export models
export {
  UserModel,
  AccountModel,
  TransactionModel,
  BudgetModel,
  GoalModel
};

// TODO: Implement the following models:
// - user.model.ts
// - account.model.ts
// - transaction.model.ts
// - budget.model.ts
// - goal.model.ts

/**
 * This file serves as the main entry point for all database models in the backend.
 * It imports and re-exports all model definitions, providing a centralized location
 * for accessing models throughout the application.
 */

// Human tasks:
// - Ensure that all model files (user.model.ts, account.model.ts, transaction.model.ts, budget.model.ts, goal.model.ts) are created and properly defined
// - Verify that the model definitions align with the database schema and application requirements
// - Consider adding any additional models that may be required for the application's functionality