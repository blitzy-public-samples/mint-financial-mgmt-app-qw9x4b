/**
 * This file defines constants for PostgreSQL table names used in the Mint Replica application.
 * These constants should be used throughout the application to ensure consistency
 * and make it easier to update table names if needed in the future.
 */

export const USERS = 'users';
export const ACCOUNTS = 'accounts';
export const TRANSACTIONS = 'transactions';
export const BUDGETS = 'budgets';
export const GOALS = 'goals';

/**
 * TODO: Human tasks
 * 1. Verify that the table names match the actual PostgreSQL schema design
 * 2. Ensure that these table names are used consistently across the application
 */