NODE_ENV=development

# Server
PORT=3000

# Database
DATABASE_URL=postgresql://username:password@localhost:5432/mint_replica
MONGODB_URI=mongodb://username:password@localhost:27017/mint_replica

# Redis
REDIS_URL=redis://localhost:6379

# Authentication
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRATION=1d

# External Services
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret
PLAID_ENV=sandbox

CREDIT_BUREAU_API_KEY=your_credit_bureau_api_key
INVESTMENT_DATA_API_KEY=your_investment_data_api_key

# Email
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
FROM_EMAIL=noreply@mintreplica.com

# Logging and Monitoring
SENTRY_DSN=your_sentry_dsn
LOG_LEVEL=info

# Human Tasks:
# 1. Replace placeholder values with actual development environment values
# 2. Ensure all sensitive information is kept secret and not committed to version control
# 3. Create separate .env files for different environments (development, staging, production)