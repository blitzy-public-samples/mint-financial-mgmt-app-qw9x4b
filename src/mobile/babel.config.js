module.exports = {
  presets: ['module:metro-react-native-babel-preset'],
  plugins: [],
};

// Human tasks:
// TODO: Review and update Babel configuration if needed for any specific project requirements